{
  "name": "daily-tracker-server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "dev:trpc": "node --watch src/trpc/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "express": "^4.18.0",
    "better-sqlite3": "^9.4.0",
    "drizzle-orm": "^0.30.0",
    "@trpc/server": "^10.45.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "vitest": "^1.3.0",
    "drizzle-kit": "^0.20.0"
  }
}
